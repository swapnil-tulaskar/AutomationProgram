# Suppress Selenium ChromeDriver logs
webdriver.chrome.silentOutput=true

# SLF4J logging level (only errors)
org.slf4j.simpleLogger.defaultLogLevel=error

# Disable internal HTTP client debug logs
jdk.internal.httpclient.debug=false
